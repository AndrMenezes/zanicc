% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index_metrics.R
\name{recovery_metrics}
\alias{recovery_metrics}
\alias{compute_frob}
\alias{compute_abs_diff}
\alias{compute_coverage}
\alias{compute_kl_simplex}
\alias{compute_kl_prob}
\alias{compute_js}
\alias{compute_hellinger}
\title{Parameter recovery metrics for count-compositional models}
\usage{
compute_frob(true_values, estimates)

compute_abs_diff(true_values, estimates)

compute_coverage(true_values, estimates_lo, estimates_up)

compute_kl_simplex(true_values, estimates, ep = 1)

compute_kl_prob(true_values, estimates)

compute_js(true_values, estimates)

compute_hellinger(true_values, estimates)
}
\arguments{
\item{true_values}{A matrix containing the true values of the model parameter
being evaluated. Rows correspond to observations and columns
correspond to categories.}

\item{estimates}{A matrix containing posterior point estimates (posterior
means or medians) of the corresponding parameter. Must have the same
dimensions as \code{true_values}.}

\item{estimates_lo}{A matrix containing lower posterior credible interval
intervals for the parameter estimates. Must have the same dimensions as
\code{true_values}.}

\item{estimates_up}{A matrix containing upper posterior credible interval
intervals for the parameter estimates. Must have the same dimensions as
\code{true_values}.}

\item{ep}{A small positive constant used in \code{compute_kl_simplex()} to avoid
undefined values when the true probability is positive but the estimated
probability is zero. Defaults to \code{1.0}.}
}
\value{
A numeric value or vector containing the recovery metric. The returned value
measures the discrepancy between the true parameter values and their
posterior estimates; smaller values indicate better recovery, while coverage
values closer to the nominal credible level indicate better calibration.
}
\description{
A collection of metrics to assess the performance of count-compositional
models in recovering their parameters from simulated data sets. See below for
further details.
}
\details{
The functions are designed for evaluating parameter recovery in
count-compositional models. The appropriate metric depends on the type of
parameter being assessed.
\subsection{Divergence-based metrics}{
\itemize{
\item \code{compute_kl_simplex()}: Kullback-Leibler divergence between probability
vectors defined on the simplex.
\item \code{compute_kl_prob()}: Kullback-Leibler divergence for scalar probability parameters.
\item \code{compute_js()}: Jensen-Shannon divergence between probability vectors.
}
}

\subsection{Distance-based metrics}{
\itemize{
\item \code{compute_hellinger()}: Hellinger distance between true values and posterior
estimates.
\item \code{compute_frob()}: Frobenius norm between true values and posterior
estimates.
\item \code{compute_abs_diff()}: Mean absolute difference between true values and
posterior estimates.
}
}

\subsection{Posterior uncertainty}{
\itemize{
\item \code{compute_coverage()}: Compute the empirical coverage given the crebible inteval
of the parameters.
}

In the simulation studies conducted in Menezes et al. (2025), we assessed
and compared different models with respect to their ability to estimate the
following parameters:

\describe{
\item{population-level count probabilities, \eqn{\theta_{ij}} }{
It provides the information underlying the observed compositional counts.
The vector \eqn{\pmb{\theta}_{i} = (\theta_{i1}, \ldots, \theta_{id}) \in \mathbb{S}^d}
lie in the continuous simplex space
\eqn{\mathbb{S}^d=\{\bm{\theta}\in\mathbb{R}^d; \theta_{ij} > 0, \sum_{j=1}^d \theta_{ij}=1\}}.

For these parameters, we use the Kullback-Leibler divergence for
parameters on the simplex, averaged over the observations.
This is implemented in the function \code{compute_kl_simplex()}.

}

\item{population-level structural zeros probabilities, \eqn{\zeta_{ij}} }{
It provides the information on the probability a given observation \eqn{i} of
category \eqn{j} is structural zero.
Each \eqn{\zeta_{ij} \in [0, 1]}.

For these parameters, we use the Kullback-Leibler divergence averaged over
the observations, implemented in the function \code{compute_kl_prob()}.

}

\item{individual-level structural zero probabilities, \eqn{\vartheta_{ij}}}{
It describe within- and between-subject heterogeneity, while \eqn{\bm{\theta}_i}
characterises the counts at a global level.
The vector \eqn{\pmb{\vartheta}_{i} = (\vartheta_{i1}, \ldots, \vartheta_{id}) \in \mathbb{S}^d}
also lie in the continuous simplex space
\eqn{\mathbb{S}^d=\{\bm{\vartheta}\in\mathbb{R}^d; \vartheta_{ij} \geq 0, \sum_{j=1}^d \vartheta_{ij}=1\}},

However, note that \eqn{\vartheta_{ij}} can have spikes at zero.
Because of this, we use the Jensen-Shannon divergence
averaged over the observations for these parameters, implemented in the function \code{compute_js}.
}
}
}
}
\references{
Menezes, A. F. B., Parnell, A. C. and Murphy, K. (2026), Bayesian nonparametric models for zero-inflated
count-compositional data using ensembles of regression trees. \url{https://arxiv.org/abs/2601.08067}
}
