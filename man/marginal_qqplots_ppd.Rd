% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{marginal_qqplots_ppd}
\alias{marginal_qqplots_ppd}
\alias{plot_qqplots_ppd}
\alias{marginal_quantiles_ppd}
\title{Marginal QQ-plots from the posterior predictive distribution}
\usage{
plot_qqplots_ppd(
  Y,
  Y_ppc = NULL,
  object = NULL,
  relative = FALSE,
  output = FALSE,
  len_probs = 100L,
  mfrow = NULL
)

marginal_quantiles_ppd(yj_ppc, probs)
}
\arguments{
\item{Y}{A matrix of observed count-compositional data with samples in rows and
categories in columns.}

\item{Y_ppc}{A three-dimensional array containing the posterior predictive
distribution. The first dimension indexes the posterior samples, while the
remaining dimensions correspond to replicated count-compositional matrices.
If \code{NULL}, then the \code{plot_qqplots_ppd} function generates the posterior
predictive distribution from the model \code{object}.}

\item{object}{A fitted model object of class \code{zanicc} used to generate posterior
predictive samples via \code{ppd()}. Only used when \code{Y_ppc = NULL}.}

\item{relative}{Logical. If \code{TRUE}, QQ-plots are computed using relative
compositions instead of counts. Default is \code{FALSE}.}

\item{output}{Logical. If \code{TRUE}, return the data used to construct the
QQ-plots in addition to producing the plots. Default is \code{FALSE}.}

\item{len_probs}{Integer giving the number of equally spaced probabilities
used to compute the quantiles. Default is \code{100}.}

\item{mfrow}{A length-two integer vector passed to \code{par(mfrow)} specifying
the layout of the QQ-plots. If \code{NULL} (default), a suitable layout is
computed using \code{grDevices::n2mfrow()}.}

\item{yj_ppc}{A matrix containing the posterior predictive samples for a single
category, with rows corresponding the samples and columns to observations.}

\item{probs}{A numeric vector of probabilities at which marginal quantiles
are computed.}
}
\value{
\code{plot_qqplots_ppd()} is called for its side effect of producing the marginal
QQ-plots for all categories. If \code{output = TRUE}, it returns a list whose
\eqn{j}-th element contains the empirical quantiles and the corresponding
posterior predictive median, lower (2.5\%), and upper (97.5\%) quantiles used
to construct the QQ-plot for category \eqn{j}.

\code{marginal_quantiles_ppd()} returns a matrix with one row per probability in
\code{probs} and three columns containing the posterior predictive median, lower
(2.5\%), and upper (97.5\%) quantiles.
}
\description{
Compute and visualise the marginal QQ-plots from the posterior predictive
distribution of a given fitted count-compositional model.
The function \code{plot_qqplots_ppd()} compares the empirical marginal quantiles
of each category with the corresponding posterior predictive quantiles,
together with pointwise 95\% posterior predictive intervals.
The helper function \code{marginal_quantiles_ppd()} computes the posterior predictive
marginal quantiles used to construct these QQ-plots for a given category.
}
