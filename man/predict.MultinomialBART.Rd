% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.MultinomialBART}
\alias{predict.MultinomialBART}
\title{Posterior predictions}
\usage{
\method{predict}{MultinomialBART}(
  object,
  newdata,
  ndpost = object$ndpost,
  output_dir = tempdir(),
  load = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A fitted model object. Supported classes include
\code{MultinomialBART}, \code{MultinomialLNBART}, \code{ZANIMBART},
\code{ZANIMLNBART}, \code{DMRegression}, and  \code{ZANIDMRegression}.}

\item{newdata}{A matrix or data frame containing the covariates for which
predictions are required. The columns must correspond to the predictors
used when fitting the model.}

\item{ndpost}{Number of posterior draws to use for prediction. Default is
\code{object$ndpost}.}

\item{output_dir}{Directory in which the predictions across all posterior draws
are written. The resulting binary files are named according to the predicted
quantity, for example \code{theta_ij.bin} for \code{type="theta"} or
\code{zeta_ij.bin} for \code{type="zeta"}. Default is \code{tempdir()}.}

\item{load}{Logical indicating whether prediction draws written to binary
files should be loaded into R memory. Defaults to \code{TRUE}. Set to
\code{FALSE} when predictions are too large to comfortably fit in memory.}

\item{verbose}{Logical indicating whether progress should be displayed.
Defaults is \code{TRUE}.}

\item{type}{The type of prediction to return. For \code{ZANIMBART} and
\code{ZANIMLNBART}, choices are \code{"theta"} and \code{"zeta"}. For
\code{DMRegression}, choices are \code{"alpha"} and \code{"theta"}. For
\code{ZANIDMRegression}, choices are \code{"alpha"}, \code{"zeta"}, and
\code{"theta"}.}
}
\value{
Posterior prediction draws. When \code{load = TRUE}, the draws are
loaded into R memory. When \code{load = FALSE}, the prediction draws are
written to the files in \code{output_dir}.
}
\description{
Compute the posterior predictions for the ML-BART, MLN-BART, ZANIM-BART,
ZANIM-LN-BART, DM-reg and ZANIDM-reg models for a given newdata across all the
posterior draws of model parameters.
}
\details{
For BART-based models, the predictions can be written to binary files on
disk. The \code{load} argument controls whether these draws are loaded into
R memory.
}
