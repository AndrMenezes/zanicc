% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inverse_posterior.R
\name{runifconvexhull}
\alias{runifconvexhull}
\title{Generate uniform random points from the the convex hull of set of points}
\usage{
runifconvexhull(n, X)
}
\arguments{
\item{n}{Integer giving the number of points to generate.}

\item{X}{A numeric \eqn{n \times p} matrix representing a set of points in
\eqn{\mathbb{R}^p}. Each row is a point and each column is a coordinate.}
}
\value{
A numeric \eqn{n \times p} matrix containing independent samples uniformly
distributed over the convex hull of the points in \code{X}.
}
\description{
Generate random points uniformly distributed over the convex hull of a set of
points in Euclidean space.
}
\details{
The function samples uniformly from the convex hull of the points in \code{X}.
The algorithm proceeds as follows:
\enumerate{
\item The convex hull is partitioned into non-overlapping simplices using
the Delaunay triangulation computed by \code{\link[geometry:delaunayn]{geometry::delaunayn()}}.

\item The volume of each simplex is computed. A simplex is selected with
probability proportional to its volume.

\item A point is sampled uniformly within the selected simplex:
\itemize{
\item Uniform barycentric coordinates are generated by sampling from a
Dirichlet distribution with parameters
\eqn{(1,\ldots,1)}. This is implemented by normalising independent
exponential random variables.
\item The barycentric coordinates are transformed into Cartesian
coordinates using the vertices of the selected simplex.
}
}

This implementation follows the method described by Stack Exchange user  \strong{whuber}:
\url{https://stats.stackexchange.com/questions/663559/transform-convex-hull-vertex-weights-for-uniform-distribution/663594#663594}.
}
