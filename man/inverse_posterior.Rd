% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inverse_posterior.R
\name{inverse_posterior}
\alias{inverse_posterior}
\alias{inverse_posterior_zanimlnbart}
\title{Sample from the inverse posterior distribution of past climate variables}
\usage{
inverse_posterior_zanimlnbart(
  object,
  Y,
  method = c("sir", "ess", "cess"),
  dir_posterior_fx = NULL,
  x_proposal = NULL,
  ndpost = object$ndpost,
  nburnin = 1L,
  n_particles = NULL,
  mean_prior = NULL,
  S_prior = NULL,
  X_ini = NULL,
  Amat = NULL,
  bvec = NULL,
  lower = NULL,
  upper = NULL,
  eta = 50
)
}
\arguments{
\item{object}{A fitted ZANIM-LN-BART model of class \code{zanicc}. The model must
have been fitted using a modern calibration data set.}

\item{Y}{A matrix of fossil count-compositional observations with samples in
rows and taxa in columns. The taxa (columns) must match those used in the modern
data set.}

\item{method}{Character string specifying the sampling algorithm. One of
\code{"sir"} (the default), \code{"ess"}, or \code{"cess"}.}

\item{dir_posterior_fx}{Directory used by the \code{"sir"} method to store (or
retrieve) the taxon-specific posterior tree ensembles required for likelihood
evaluation.}

\item{x_proposal}{Proposal samples used by the \code{"sir"} method. Each row
corresponds to a proposed climate vector.}

\item{ndpost}{Number of posterior draws from the fitted ZANIM-LN-BART model
to use. Must not exceed \code{object$ndpost}.}

\item{nburnin}{Number of burn-in iterations for the \code{"ess"} and \code{"cess"}
methods. For each posterior draw of the forward model, the ESS/cESS sampler
is run for \code{nburnin} iterations before collecting a sample. The default
(\code{nburnin = 1}) corresponds to the naive cut-posterior algorithm.}

\item{n_particles}{Number of particles, that is, samples used to obtain an unbiased estimate of
the ZANIM-LN-BART likelihood.}

\item{mean_prior, S_prior}{Prior mean vector and covariance matrix, respectively,
for the climate variables used for the \code{ess} and \code{cess} methods.}

\item{X_ini}{Initial climate values used to initialise the ESS/CESS samplers. The default is \code{NULL},
which then uses a sample from the prior.}

\item{Amat, bvec}{Halfspace representation of the convex hull, typically obtained via the output of \code{\link[geometry:convhulln]{geometry::convhulln()}} with the flag \code{output.options="n"}.}

\item{lower, upper}{Integers giving the lower and upper limits to constrain the
past climate.}

\item{eta}{Tuning parameter using in the \code{cess} algorithm related to the logistic
approximation of the indicator function. Defaults to \code{50.0}.}
}
\description{
Approximates the inverse posterior distribution of the unobserved climate
variables given the corresponding with compositional fossil counts, and the
posterior draws of parameters from the ZANIM-LN-BART model.

This is the main quantity of interest when performing pollen-based palaeoclimate
reconstruction using the ZANIM-LN-BART model.
}
\details{
This function implements the Bayesian modular framework proposed in
in Menezes et al. (2026) to perform palaeoclimate reconstruction.

The function performs inverse prediction using a previously fitted
ZANIM-LN-BART model.
The ZANIM-LN-BART model is first fitted to a modern
calibration data set in order to estimate the pollen-climate relationships

Given one or more fossil assemblages, this function uses
these posterior draws to approximate the cut posterior distribution of the
corresponding latent climate variables.

Several sampling algorithms are available, including sampling-importance-
resampling (SIR), elliptical slice sampling (ESS), and constrained
elliptical slice sampling (CESS).

This function is a high-level interface to an efficient \verb{C++} implementation.
}
