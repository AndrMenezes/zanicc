% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inverse_posterior.R
\name{inverse_posterior_mlbart}
\alias{inverse_posterior_mlbart}
\title{Sample the inverse posterior distribution using the ML-BART model.}
\usage{
inverse_posterior_mlbart(
  object,
  Y,
  method = c("is", "gibbs"),
  proposal_parms = NULL,
  ess_parms = NULL,
  n_proposal = 5000L,
  sir = FALSE,
  n_resampling = floor(n_proposal/2),
  dir_posterior_fx = NULL,
  save = FALSE,
  output_dir = tempdir(),
  x_proposal = NULL
)
}
\arguments{
\item{object}{an object of class MultinomialBART.}

\item{Y}{matrix with the compositional counts to compute the inverse posterior of x*.}

\item{method}{which sampler method to use. Options are \code{is} (importance sampling)
and \code{gibbs} (two-stage Gibbs sampler).}

\item{proposal_parms}{list with the parameters to simulate from a uniform proposal
when performing the importance sampling.
If X is one-dimensional the list should contain the range with entries \code{min_x} and
\code{max_x}.
Otherwise the list should contain an entry \code{X} with a matrix n by p of
representing the observed covariates.
Then, the proposal distribution is an uniform over the convex hull of
observed \code{X}.}

\item{ess_parms}{list with parameters that control the elliptical slice sampling (eSS).
using for the \code{gibbs} method. The list should contain the following entries.
\code{mean_prior}: prior mean vector using in the eSS.
\code{S_prior}: prior covariance matrix using in the eSS.
\code{n_rep}:  number of times to repeat one iteration of eSS.
\code{X_ini}: (optional) matrix with the initial value for the eSS. If not passed, then
sample from the multivariate normal prior.}

\item{n_proposal}{integer indicating the number samples from the proposal distribution.}

\item{sir}{logical. Whether perform sampling importance resampling with replacement.}

\item{n_resampling}{integer. Number of samples to resample. Default is \code{floor(n_proposal/2)}.}

\item{dir_posterior_fx}{valid character folder to read both the simulated
proposal values \eqn{x^\ast} and the posterior-predictive distribution
\eqn{f(x^\ast)}. Default is \code{NULL}.}

\item{save}{logical, whether to save the simulated proposal values for \eqn{x^\ast} in the \code{output_dir} folder.}

\item{output_dir}{valid character folder to write the posterior-predictive distribution \eqn{f(x^\ast)}.
Default is \code{tempdir()}.}

\item{x_proposal}{matrix with the proposal distribution, if the user wants to
give a different proposal distribution. Default is \code{NULL}.}
}
\description{
Sample the inverse posterior distribution using the ML-BART model.
}
