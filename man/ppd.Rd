% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_predictive.R
\name{ppd}
\alias{ppd}
\alias{ppd.MultinomialBART}
\alias{ppd.MultinomialLNBART}
\alias{ppd.ZANIMBART}
\alias{ppd.ZANIMLNBART}
\alias{ppd.ZANIMLNRegression}
\alias{ppd.ZANIMRegression}
\alias{ppd.ZANIDMRegression}
\title{Posterior predictive distribution}
\usage{
ppd(object, relative = FALSE, ...)

\method{ppd}{MultinomialBART}(
  object,
  relative = FALSE,
  in_sample = TRUE,
  draws_theta = NULL,
  n_trials = NULL,
  output_dir = NULL,
  n_pred = NULL,
  ndpost = object$ndpost,
  batch_size = 50L,
  printevery = 100L,
  ...
)

\method{ppd}{MultinomialLNBART}(
  object,
  relative = FALSE,
  conditional = FALSE,
  in_sample = TRUE,
  draws_theta = NULL,
  draws_chol_Sigma_V = NULL,
  n_trials = NULL,
  output_dir = NULL,
  n_pred = NULL,
  ndpost = object$ndpost,
  batch_size = 50L,
  printevery = 100L,
  ...
)

\method{ppd}{ZANIMBART}(
  object,
  relative = FALSE,
  conditional = FALSE,
  in_sample = TRUE,
  draws_theta = NULL,
  draws_zeta = NULL,
  n_trials = NULL,
  output_dir = NULL,
  n_pred = NULL,
  ndpost = object$ndpost,
  batch_size = 50L,
  printevery = 100L,
  ...
)

\method{ppd}{ZANIMLNBART}(
  object,
  relative = FALSE,
  conditional = FALSE,
  in_sample = TRUE,
  draws_theta = NULL,
  draws_zeta = NULL,
  draws_chol_Sigma_V = NULL,
  n_trials = NULL,
  output_dir = NULL,
  n_pred = NULL,
  ndpost = object$ndpost,
  batch_size = 50L,
  printevery = 100L,
  ...
)

\method{ppd}{ZANIMLNRegression}(object, relative = FALSE, conditional = FALSE, printevery = 100L, ...)

\method{ppd}{ZANIMRegression}(object, relative = FALSE, conditional = FALSE, printevery = 100L, ...)

\method{ppd}{ZANIDMRegression}(
  object,
  relative = FALSE,
  conditional = FALSE,
  in_sample = TRUE,
  draws_alpha = NULL,
  draws_zeta = NULL,
  n_trials = NULL,
  ndpost = object$ndpost,
  printevery = 100L,
  ...
)
}
\arguments{
\item{object}{A fitted model object. The supported model classes are
\code{MultinomialBART}, \code{MultinomialLNBART}, \code{ZANIMBART}, \code{ZANIMLNBART},
\code{DMRegression}, \code{ZANIMRegression}, \code{ZANIMLNRegression} and  \code{ZANIDMRegression}.
Use \code{\link[=zanicc]{zanicc()}} for fitting such models.}

\item{relative}{Logical. If \code{TRUE}, return the posterior predictive draws
as continuous compositions, obtained by normalising the simulated compositional
counts by their sample-specific number of trials. If \code{FALSE}, return
compositional counts. Default is \code{FALSE}.}

\item{...}{Currently not used.}

\item{in_sample}{Logical. If \code{TRUE}, generate the posterior predictive
distribution for the samples used to fit the model, using the
corresponding posterior draws stored in the model \code{object}.
If \code{FALSE}, the posterior predictive distribution is simulated for a potential
new data set. In this case,
the sample-specific posterior draws must either be supplied explicitly
through the relevant \verb{draws_\{*\}} arguments or loaded from binary files saved in
\code{output_dir}.
Default is \code{TRUE}.}

\item{draws_theta, draws_zeta, draws_alpha, draws_chol_Sigma_V}{Array with the sample-specific posterior draws of the population-level
parameters for the corresponding model.
Used for out-of-sample posterior predictive distributions when the draws are
supplied directly rather than loaded from the path \code{output_dir}.}

\item{n_trials}{Integer vector giving the sample-specific number of
trials for the new samples. Required when \code{in_sample = FALSE}.
The length of \code{n_trials} should correspond to the number of observations
in the new data.}

\item{output_dir}{Character string giving the directory containing
posterior draws of sample-specific parameters for the new data.
In particular, the directory is expected to contain the files required
to reconstruct the relevant \verb{draws_*} objects, such as
\code{theta_ij.bin} and, for zero-inflated models, \code{zeta_ij.bin}.
For logistic-normal models, the posterior draws of the covariance
parameters are loaded from the model's forest path.}

\item{n_pred}{Integer giving the number of samples in the new data.
This argument is required when posterior draws for the new samples
are loaded from files rather than supplied directly through the
\verb{draws_*} arguments.}

\item{ndpost}{Integer giving the number of posterior draws to use.
Default is \code{object$ndpost}. This argument is used for out-of-sample
prediction when posterior draws are loaded from files.}

\item{batch_size}{Integer giving the number of observations processed
in each batch when posterior draws are loaded from disk. Batching can
reduce memory usage for large data sets. Default is \code{50} samples
per batch.}

\item{printevery}{Integer giving the frequency with which progress
information is printed during posterior predictive sampling. Default is to print
at each \code{100} sample.}

\item{conditional}{Logical. Determines whether the posterior predictive
distribution is generated conditionally on the individual-level
multinomial probabilities or the population-level parameters.
If \code{TRUE}, the predictive counts are generated from a multinomial
distribution using the posterior draws of the individual-level
probabilities.
If \code{FALSE}, the predictive distribution is generated from the sampling
distribution implied by the fitted model and its population-level
parameters.
Default is \code{FALSE}.}
}
\value{
A three-dimensional array containing draws of the posterior predictive distribution
of the corresponding model with dimensions
\eqn{M \times n \times d}, where \eqn{M} is the
number of posterior draws, \eqn{n} is the number of observations, and
\eqn{d} is the number of categories.
}
\description{
Generate draws from the posterior predictive distribution of the data
for different count-compositional models.
These draws can then be used to perform posterior predictive checks.
}
\details{
The posterior predictive distribution can be generated either
by simulating from a multinomial distribution condition on the individual-level
count probabilities  or using the underlying sampling distribution of the model
and its posterior draws of the population-level parameters.

When \code{conditional = TRUE}, for each posterior draw
\eqn{k \in \{1,\ldots, M\} } and sample \eqn{i \in \{ 1,\ldots,n\}},
posterior predictive distribution simulated as follows
\deqn{\left(Y^{(k)}_{i} \mid N_i, \boldsymbol{\vartheta}^{(k)}_{i}\right) \sim \text{Multinomial}\lbrack N_i, \boldsymbol{\vartheta}^{(k)}_{i} \rbrack,}
where \eqn{N_i} is the number of trials for observation \eqn{i}
and \eqn{\boldsymbol{\vartheta}^{k}_{i}} are the posterior draws of the
individual-level count probabilities.

For out-of-sample prediction (\code{in_sample = FALSE}), posterior draws of
the sample-specific parameters can either be supplied directly through
the appropriate \verb{draws_*} argument(s), or loaded in batches from
\code{output_dir} path. The latter approach avoids loading all posterior draws
into memory simultaneously, reducing the memory usage.
}
\seealso{
\code{\link[=predict]{predict()}}, \code{\link[=plot_qqplots_ppd]{plot_qqplots_ppd()}}
}
